# Defaults for hactl
# Copied into /etc/hactl/01-defaults.yml
paths:
  venv: /henv
  data: /hdata
user:
  name: dev
  password: dev
lovelace:
  plugins: []
  extra_files: []
logging:
  colors:
    - pattern: '.*ERROR.*'
      line_color: red
    - pattern: '.*WARNING.*'
      line_color: yellow
